{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        // Trigger wasmserve reload
        "match": "\\.(go|png|wav|ogg|ttf)$",
        "cmd": "wget -q -O /dev/null localhost:8080/_notify"
      }
    ]
  }
}
